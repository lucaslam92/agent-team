# Semantic Store

本目录存放结构化语义知识，而不是长篇文档知识。

结构：

- `generated/`：候选卡片、中间产物、merge report、review queue
- `normalized/`：正式结构化知识
- `index/`：graph index
- `state/`：source registry、promotion state、dedupe index

边界：

- 长期规则、业务背景、ADR 请放在 `knowledge/`
- 当前任务输入输出请放在 `artifacts/`
