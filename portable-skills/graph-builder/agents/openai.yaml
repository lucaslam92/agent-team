interface:
  display_name: "Graph Builder"
  short_description: "Build graph indexes from engineering signals"
  default_prompt: "Use $graph-builder to build nodes.json and edges.json from extracted signals."
